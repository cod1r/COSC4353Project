{
  "jest": {
    "setupFiles": [
      "dotenv/config"
    ],
    "collectCoverage": true
  },
  "scripts": {
    "test": "jest",
    "start": "DOTENV_CONFIG_PATH=./.env node -r dotenv/config app.js",
    "format": "prettier --write ."
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-session": "^1.17.2",
    "jsonwebtoken": "^8.5.1",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "prettier": "^2.6.2"
  }
}
